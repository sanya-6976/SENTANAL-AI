"""
Sentinel AI — ETL Pipeline Orchestrator

Coordinates the Extract → Transform → Load → Validate workflow.
Supports full and incremental pipeline runs with logging and error handling.

Implementation: Sprint 4
"""

from __future__ import annotations

# Module will be implemented in Sprint 4
# Planned functions:
#   - run_full_pipeline(config) -> PipelineResult
#   - run_incremental_pipeline(config, since) -> PipelineResult
#   - run_single_entity(entity_name, config) -> PipelineResult
