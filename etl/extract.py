"""
Sentinel AI — Data Extraction Module

Extracts raw data from various sources (CSV, JSON, PostgreSQL, APIs)
and delivers it as pandas DataFrames for the transform stage.

Implementation: Sprint 3
"""

from __future__ import annotations

# Module will be implemented in Sprint 3
# Planned functions:
#   - extract_from_csv(filepath, schema) -> pd.DataFrame
#   - extract_from_json(filepath, schema) -> pd.DataFrame
#   - extract_from_postgres(query, connection) -> pd.DataFrame
#   - extract_from_api(endpoint, params) -> pd.DataFrame
